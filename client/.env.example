# client/.env.example — copy to client/.env. Only VITE_-prefixed vars reach the
# browser bundle, so NEVER put a secret here (§6.0).

# Base URL for the axios client (optional, defaults to http://localhost:3000).
VITE_API_URL=http://localhost:3000
